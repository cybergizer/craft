<div class="speakers__profile">
  <!-- TODO: add the bio -->
  {% capture card_contents %}
    <div class="profile__container">
      <div class="profile__background"></div>
      <div class="profile__image">
        <img
          src="{{ image }}"
          alt="{{ community.name }}"
          loading="lazy"
        />
      </div>
      {% if community.category == "keynote" %}
        <div class="profile__keynote">
          KEYNOTE
        </div>
      {% elsif community.category == "mc" %}
        <div class="profile__mc">
          <abbr title="Master of ceremonies">MC</abbr>
        </div>
      {% endif %}
    </div>
    <span class="profile__name">
      {{ community.name }}
    </span>
    {% if content %}<span class="profile__talk_title">{{ content }}</span>{% endif %}
  {% endcapture %}
  {% assign url_or_twitter = community.twitter | default: community.url %}
  {% assign url = url_or_twitter | default: community.github %}
  {% if url != blank %}
    <a
      href="{{ url }}"
      class="profile profile--keynote"
    >
      {{ card_contents | raw }}
    </a>
  {% else %}
    <div class="profile profile--{{community.category}}">
      {{ card_contents | raw }}
    </div>
  {% endif %}
</div>
